import React from "react";
import { Text, StyleSheet, View } from "react-native";

const TrackDetailScreen = () => {
  return (
    <View>
      <Text style={{ fontSize: 48 }}>Task Detail Screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default TrackDetailScreen;
